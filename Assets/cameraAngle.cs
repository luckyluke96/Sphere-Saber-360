using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class cameraAngle : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // Camera camera = Camera.main;
        // Vector3 camPosition = camera.ViewportToWorldPoint(new Vector3(0, 0, camera.nearClipPlane));
    }

    // Update is called once per frame
    void Update()
    {
        // gameObject.GetComponent<Text>().text = "hi";
        // gameObject.GetComponent<Text>().text = "cam euler rot: " + Camera.main.transform.localEulerAngles.y + "\n" +
        //    "cam loc rot: " + Camera.main.transform.rotation.y;
    }
}
